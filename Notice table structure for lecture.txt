CREATE TABLE LecNotice (
    Notice_id VARCHAR(15) NOT NULL PRIMARY KEY,
    Title VARCHAR(100) NOT NULL,
    Description TEXT NOT NULL,
    Posted_By VARCHAR(45) NOT NULL,
    Posted_Date DATE NOT NULL
);

INSERT INTO LecNotice VALUES (
    'N001',
    'Exam Schedule',
    'Final exam will be held on May 15th',
    'Mahesh',
    CURDATE()
);
